void setup() {
 // sets the background and the size of the screen
 size(1000, 1000);
 background(0);
}

void draw() {
  stroke(255);
  strokeWeight(4);
  line(mouseX, mouseY, mouseX, mouseY);

//erase when spacebar is pressed
if(keyPressed)
if(key==' ') ;
background(0);
}

 
  